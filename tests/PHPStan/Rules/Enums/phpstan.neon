parameters:
    level: max
    paths:
        - Fixtures
    
    # Test that the CaseMustBePascal rule is working
    # ValidPascalCaseEnum.php should have 0 errors
    # InvalidPascalCaseEnum.php should have 7 errors (one per invalid case)

includes:
    - ../../../../../phpstan.neon
